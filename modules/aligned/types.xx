module;

#include <vector>

export module aligned:types;

import :allocator;

export namespace aligned {

template <typename T, std::size_t alignBytes = 64>
using vector = std::vector<T, Allocator<T, alignBytes>>;

using avx2_vector = vector<double, 64>;

} // <-- namespace aligned
